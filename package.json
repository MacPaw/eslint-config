{
  "private": "true",
  "workspaces": [
    "eslint-config-base",
    "eslint-config-react",
    "eslint-config-typescript"
  ],
  "scripts": {
    "patch": "yarn workspaces run version:patch"
  }
}
